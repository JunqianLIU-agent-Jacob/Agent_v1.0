    目标是写一个循环多轮对话，聊天记录储存在pysql库中的代码
    db文件是pysql库的函数模块
    utils是正则化的函数模块
    main是主程序，用了__name__进行了封装
    亮点：
    1，db的sql代码部分用了try——except——finally进行封装即使出现网络卡顿等现象，也会关闭光标和库
    2，utils库里用了一个正则化函数，可以进行对敏感词的处理
    3，main主文件中，我用了while循环，实现了代码的重复对话
    4，main主文件中，我用了if——elif循环，绑定了对话历史和退出快捷键
    5，main主文件中，我用了requests，调用了天气api，并将天气的信息转交给了ai，让ai做出处理
    6，模块化
    纠结的点：
    1，在循环中，不知道怎么让ai吃下api调用的数据，
    2，如何进行抓取关键词，例如地名
    解决方法：
    1，我直接把api调用的数据，转化成text格式，直接当成用户输入，输入进ai，可以得到ai整理后的信息
    2，利用正则化，直接抓取地名，利用循环直接调用api
    衍生问题：
    1，要想直接提取地名，正则化就需要写茫茫多的地名，非常不现实
    2，如果正则化的地名直接出现在了句子中，直接进入调用api的环节，让上下文极为割裂，所以抛弃掉了正则
    3，text的格式比较占token应该用json格式比较好

